<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Todo App</title>
  </head>
  <body>
    <div class="main w-full h-auto font-mono">
      <div
        class="form p-5 text-black h-auto m-5 rounded-lg shadow-inner border-2 border-gray-200"
      >
        <a class="text-blue-400 inline-block mb-5" href="/">Back to home</a>

        <h1 class="mb-5 text-blue-900 text-2xl font-bold">Edit task</h1>
        <form class="flex flex-col" action="/edit" method="post">
          <input
            class="pointer-events-none bg-gray-200 mb-3 px-5 py-3 w-full rounded-lg outline-none border-2 border-gray-100 text-blue-900 font-medium"
            type="text"
            placeholder="Previous title"
            name="previoustitle"
            value="<%=
          filename.split('_').join(' ').slice(0, -4) %>"
          />

          <input
            class="px-5 py-3 w-full rounded-lg outline-none border-2 border-gray-100 text-blue-900 font-medium"
            type="text"
            placeholder="New title"
            name="newtitle"
            value="<%=
          filename.split('_').join(' ').slice(0, -4) %>"
          />
          <textarea
            class="resize-none mt-3 w-full px-5 py-3 rounded-lg outline-none border-2 border-gray-100 text-blue-900"
            placeholder="Edit task details"
            name="details"
          >
<%= filedata.trim() %>
        </textarea
          >

          <input
            type="submit"
            class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-lg w-72 mt-5 px-3 py-2 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
            value="Edit task"
          />
        </form>
      </div>
    </div>
  </body>
</html>
